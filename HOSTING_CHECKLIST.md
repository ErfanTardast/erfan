# چک‌لیست قبل از انتقال Keyvan Rice به هاست واقعی

## 1. تصمیم هاست و مسیر انتشار

- اگر سایت روی دامنه اصلی مثل `https://keyvanrice.ir` می‌رود، build باید بدون `GITHUB_PAGES=true` انجام شود تا سایت در ریشه دامنه بالا بیاید.
- اگر همچنان روی GitHub Pages با مسیر `/erfan` می‌ماند، build فعلی با `GITHUB_PAGES=true` درست است.
- برای هاست واقعی پیشنهاد عملی: Vercel یا یک VPS/Node host. GitHub Pages فقط برای نسخه static خوب است و backend واقعی ندارد.

## 2. دامنه و تنظیمات پایه

- خرید/اتصال دامنه `keyvanrice.ir` یا دامنه نهایی برند.
- تنظیم `NEXT_PUBLIC_SITE_URL=https://keyvanrice.ir` برای sitemap و robots.
- فعال کردن SSL.
- تست redirectهای `www` و بدون `www`.

## 3. پرداخت و سفارش واقعی

- انتخاب درگاه پرداخت ایرانی مثل زرین‌پال، آیدی‌پی یا Pay.ir.
- ساخت backend برای ثبت سفارش، پرداخت، تأیید تراکنش و وضعیت سفارش.
- checkout فعلی UI کامل دارد، اما پرداخت و ذخیره سفارش واقعی هنوز backend ندارد.

## 4. حساب کاربری واقعی

- حساب کاربری فعلی client-side و داخل مرورگر ذخیره می‌شود.
- برای هاست واقعی باید auth واقعی اضافه شود: Supabase، Firebase، یا backend اختصاصی.
- سفارش‌ها، آدرس‌ها و وضعیت ارسال باید در دیتابیس ذخیره شوند.

## 5. محتوا و اعتماد

- عکس‌های فعلی بهتر و مرتبط‌تر شده‌اند، اما هنوز stock هستند. برای لانچ حرفه‌ای، عکس واقعی محصول/بسته‌بندی کیوان بهترین ارتقاست.
- شماره تماس، آدرس، اینستاگرام، تلگرام و ایمیل باید واقعی شوند.
- قوانین ارسال/بازگشت/حریم خصوصی باید با شرایط واقعی فروشگاه بازبینی شوند.

## 6. SEO و انتشار

- `sitemap.xml` و `robots.txt` اضافه شده‌اند.
- عنوان و توضیحات سایت با Keyvan به‌روز شده‌اند.
- بعد از دامنه واقعی، سایت باید در Google Search Console ثبت شود.
- تصاویر محصول بهتر است alt دقیق و ترجیحاً عکس اختصاصی داشته باشند.

## 7. آنالیتیکس و مانیتورینگ

- نصب Google Analytics یا Plausible.
- ثبت رویدادهای مهم: مشاهده محصول، افزودن به سبد، شروع checkout، سفارش موفق.
- اضافه کردن error monitoring مثل Sentry برای خطاهای frontend/backend.

## 8. تست نهایی قبل از لانچ

- تست موبایل: منو، hero، product grid، checkout.
- تست مسیر کامل: محصول → سبد → checkout → سفارش.
- تست فرم تماس و خبرنامه.
- تست لینک‌های footer و mobile menu.
- تست production build با `npm run build`.

## وضعیت فعلی

- برند در UI به `Keyvan / کیوان` تغییر کرده است.
- تصاویر غیرمرتبط غذا/سالاد/سوپ با عکس‌های مرتبط با برنج، شالیزار و دانه برنج جایگزین شده‌اند.
- صفحات پشتیبانی مشتری اضافه شده‌اند: `/shipping`، `/returns`، `/faq`.
- `typecheck` و `production build` پاس می‌شوند.
- مانده اصلی برای هاست واقعی: backend، پرداخت واقعی، دیتابیس سفارش‌ها/حساب‌ها، اطلاعات تماس واقعی، دامنه و آنالیتیکس.
